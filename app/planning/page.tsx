'use client'; // Conserver cette ligne

import React from 'react'; // Conserver ou ajouter si besoin

// Toutes les autres importations sont commentées pour l'instant
// Toutes les interfaces sont commentées
// Tous les hooks (useState, useEffect, useDrop, etc.) sont commentés
// Toute la logique de fonction (fetchData, handlers, etc.) est commentée

const PlanningPage = () => {
  // Tout le contenu précédent de la fonction est commenté
  return (
    <div>
      <h1>Page Planning Simplifiée</h1>
      <p>Ceci est un test pour le build Vercel.</p>
    </div>
  );
};

export default PlanningPage; 