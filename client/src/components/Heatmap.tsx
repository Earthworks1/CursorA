import React from "react";
import { Task } from "@/types";

export default function Heatmap({ tasks, startDate, endDate, onCellClick }: { tasks: Task[], startDate: Date, endDate: Date, onCellClick: any }) {
  return <div>Heatmap temporaire (à remplacer)</div>;
} 