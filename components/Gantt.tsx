import React from "react";
import { Task } from "@/types";

export default function Gantt({ tasks, onTaskUpdate, onTaskMove }: { tasks: Task[], onTaskUpdate: any, onTaskMove: any }) {
  return <div>Gantt temporaire (à remplacer)</div>;
} 